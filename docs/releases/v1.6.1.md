# v1.6.1 — Pitch Form Layout + Review Page Horizontal Scroll

Two small polish fixes from the v1.6.0 user testing. No API changes, no new OpenSpec items, no behavior changes.

## What changed

### 1. Side-by-side date + time in pitch form

**Before (v1.6.0):** Proposed Date and Proposed Time were two stacked `form-group`s. On a 360px-wide mobile screen, the stacked pair wasted ~80px of vertical space and forced the user to scroll past the form to reach Submit.

**After (v1.6.1):** New `.form-row` CSS class wraps the two inputs in a flex row, both 50/50. The `min-width: 0` override (a standard flexbox gotcha) is the magic property that allows the inputs to shrink to fit on one line.

**Also applied to:** Submit Event form step 2 (Date + Time) — same vertical-waste problem, same fix. (v1.5.4 had stacked them; v1.6.1 brings them in line with the pitch form.)

```css
.form-row { display: flex; gap: 10px; }
.form-row .form-group { flex: 1; min-width: 0; }
```

### 2. Review page description: restore horizontal scroll

**The bug:** v1.5.5 set the review page description to `white-space: pre; overflow: auto` (horizontal scroll on long single lines). v1.5.6 changed it to `white-space: pre-wrap; word-break: break-word` to fill available vertical space. The latter change broke the original intent: long unbreakable tokens (URLs, single long words) got cut off at the right edge of the box with no way to see the full content.

The user reported: "the review page for submit event still doesn't have the horizontal scroll to view the full details."

**The fix:** v1.6.1 keeps the v1.5.6 vertical-fill behavior (`flex: 1; min-height: 0; overflow: auto`) but switches the description back to `white-space: pre` (no wrap). Long single lines now overflow the box and trigger horizontal scroll; short text uses available vertical space.

For the title and meta lines, switched from `overflow-wrap: break-word` (wraps long tokens) to `min-width: 0; max-width: 100%; overflow-x: auto; white-space: nowrap` (single line, horizontal scroll if too long). This matches the rest of the app's "horizontal scroll for long single-line content" pattern.

## Why `min-width: 0; max-width: 100%` matters

`max-width: 100%` alone is NOT enough. In a flex child, the default `min-width: auto` means the child is allowed to be as wide as its content, ignoring `max-width: 100%`. The `min-width: 0` override unlocks the shrink-to-fit behavior so the box can be narrower than its content, which is what enables `overflow-x: auto` to actually kick in.

This is a flexbox gotcha documented in the v1.5.5 release notes too.

## Scope (1 file)

`public/app.html` — 2 changes:
1. New `.form-row` + `.form-row .form-group` CSS classes (2 lines).
2. Pitch form: wrap date+time inputs in `<div class="form-row">`.
3. Submit event form step 2: same.
4. Review page description: `white-space: pre-wrap` → `white-space: pre`; add `min-width: 0; max-width: 100%`.
5. Review page title and meta: `overflow-wrap: break-word` → `min-width: 0; max-width: 100%; overflow-x: auto; white-space: nowrap`.

## Out of scope

- API patterns, data shapes, validation rules (per LEARNINGS §40, §41, §42)
- The 4 other overlays (my-stuff, details, mod, mod-event-details)
- Step 4 description textarea (v1.5.4 already correct)

## Manual test checklist

- [ ] Open pitch form: date and time are side-by-side, both fit on one row on 360px screen
- [ ] Open submit event form step 2: date and time are side-by-side
- [ ] On step 5 (review) with a long description: a long unbreakable line (URL, single long word) overflows the box and the box becomes horizontally scrollable
- [ ] On step 5 with a short description: text fills the box vertically, no scrolling needed
- [ ] Review title and meta lines scroll horizontally if too long to fit
- [ ] iOS Safari: horizontal scroll on the description is discoverable (touch + drag left/right)

## Verification

- `npm run build` → OK ✅
- `npm test` → 6/6 passed ✅
- `npm run type-check` → OK ✅

## Stats

- 1 file changed: `public/app.html`
- 1 new CSS class (`.form-row`)
- 5 element-level changes
- 0 API changes
- 0 new OpenSpec changes
- 0 breaking changes

## LEARNINGS

§46 added documenting:
- Why date+time should be side-by-side (vertical space + tap target)
- The `min-width: 0` flexbox gotcha
- Why v1.5.5 → v1.5.6 → v1.6.1 went back and forth on `pre` vs `pre-wrap` and why `pre` is correct
- Manual test checklist
